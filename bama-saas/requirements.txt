# Runtime install for Docker — keep aligned with pyproject.toml [project.dependencies].
# pytest is also listed so `docker compose exec django pytest` works without a
# second install; the canonical extra is pyproject.toml [project.optional-dependencies] test.
Django>=5.0,<6.0
djangorestframework>=3.15
drf-spectacular>=0.27
django-filter>=24.2
django-cors-headers>=4.4
dj-database-url>=2.2
psycopg[binary]>=3.2
jdatetime>=4.1
requests>=2.31
gunicorn>=22.0
numpy>=1.26
pytest>=8.2
pytest-django>=4.8
